# MyBatis XML

UserMapper.xml：账号与资料修改。CategoryMapper.xml：分类查询。MarketMapper.xml：分类管理、商品、订单、评价、私信与举报。全部业务 SQL 使用参数绑定。

字段按 Word 及用户确认的修正口径映射。订单状态采用 PDM 的 0至5。已验证解析、绑定与装配；实际 MySQL 执行、约束、并发与回滚仍待验证。没有修改或执行建表与触发器脚本。
